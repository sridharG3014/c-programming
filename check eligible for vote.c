#include<stdio.h>
int main(){
    int num;
    scanf("%d",&num);
    if(num>=18 && num<=18)
    {
        printf("Eligible for Vote");
    }
    else{
        printf("Not Eligible for Vote");
    }
        return 0;
}